/**
 * Returns a greeting for the given name.
 *
 * Public API: callers rely on the exact "Hello, <name>!" format.
 */
export function greet(name) {
  // Build the greeting string and return it.
  return `Hello, ${name}!`;
}
